(() => {
    const init = (override) => {
        'use strict';

        var args = {
        };

        Object.assign(args, override);

        console.error('Not yet implemented!')
    };

    module.exports = init;
})();